# Copy to `.env` for local development and production.

# SQLite file at repository `data/app.db` (path relative to `prisma/`).
DATABASE_URL="file:../data/app.db"

# iron-session: minimum 32 random characters (generate e.g. `openssl rand -hex 32`).
SESSION_PASSWORD="replace-with-32-or-more-random-characters-min"

# One-time / repeat seed: creates or updates the admin user and demo buildings.
SEED_ADMIN_USERNAME="admin"
SEED_ADMIN_PASSWORD="choose-a-strong-password-min-8-chars"

# Optional: GWR CSV (semicolon). Defaults to Kanton Basel-Stadt open export (subset of CH).
# For nationwide data, point this at a file or URL from your BFS MADD / cantonal bulk export.
# GWR_CSV_URL="https://example.invalid/gwr_gebaeude.csv"

# Ingest: `npm run gwr:ingest` (replace DB buildings) or `npm run gwr:ingest -- --append`
